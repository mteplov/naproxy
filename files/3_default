server {
    listen 8080;

    # Статические файлы .jpg, .png, .gif
    location /images/ {
        root /var/www/;
    }

    # Всё остальное идёт на HAProxy
    location / {
        proxy_pass http://127.0.0.1:9000;
    }
}
